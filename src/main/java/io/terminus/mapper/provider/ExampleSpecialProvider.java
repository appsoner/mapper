package io.terminus.mapper.provider;

import io.terminus.mapper.mapperhelper.MapperTemplate;
import io.terminus.mapper.mapperhelper.MapperHelper;

/**
 * Created by xgs on 16/6/30.
 */
public class ExampleSpecialProvider extends MapperTemplate {
    public ExampleSpecialProvider(Class<?> mapperClass, MapperHelper mapperHelper) {
        super(mapperClass, mapperHelper);
    }


}
